* Implement Share via 'torrent-get' magnetLink field

* Action-bar search in the torrent list

* Use style="?android:listSeparatorTextViewStyle" for
  list view headers

* Show a small indicator on each torrent row that has tracker errors
* Experiment with swipe-to-delete pattern (example from dashclock)

* Add trackers and peers to the details fragment

* Investigate creating a widget, and adding notifications
